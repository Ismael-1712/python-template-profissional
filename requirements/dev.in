# Arquivo de entrada para pip-compile gerar requirements/dev.txt
# Use: pip-compile --output-file requirements/dev.txt requirements/dev.in
#
# Dependências de desenvolvimento migradas do pyproject.toml

ruff==0.14.13
pytest==9.0.2
pytest-cov==7.0.0
pytest-xdist==3.8.0
PyYAML==6.0.3
pre-commit==4.5.1
python-semantic-release==10.5.3
tomli; python_version < '3.11'

# Type Checking
mypy==1.19.1
types-PyYAML==6.0.12.20250915
types-requests==2.32.4.20250913

# Code Complexity Analysis
radon
xenon

# TDD & Test Coverage Tools
diff-cover>=9.0.0

# Architecture & Dependency Governance (Tríade de Blindagem)
import-linter==2.9  # Enforça separação de camadas arquiteturais
deptry==0.24.0      # Detecta dependências não utilizadas
interrogate==1.7.0  # Métrica de cobertura de docstrings

# Pydantic (runtime + settings para BaseSettings)
pydantic>=2.0
pydantic-settings>=2.0

# CLI Framework
typer==0.21.0

# Internationalization (i18n)
Babel==2.17.0
tox

# Frontmatter parsing
python-frontmatter

# Mutation Testing
mutmut==3.4.0
textual==7.4.0  # Pin required by mutmut (avoid version conflicts)
tomlkit
pip-tools

# Security Scanners
bandit>=1.7.0
pip-audit>=2.7.0  # PyPA official SCA tool (replaced safety v3.x to avoid CI auth blocks)

# Machine Learning - Embeddings
sentence-transformers>=3.0.0

# Vector Database for Cortex Memory
chromadb>=0.4.0
 # CI Sync: 1767747796
